syntax = "proto3";

package example;

enum PhoneType {
  MOBILE = 0;
  HOME = 1;
  WORK = 2;
}

message Person {
  optional string name = 1;
  optional int32 id = 2;
  optional string email = 3;
  int32 age = 4;
  PhoneType phone_type = 5;
}
